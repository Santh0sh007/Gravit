{
  "name": "rewind-runner",
  "version": "1.0.0",
  "description": "Rewind Runner — A pixel-art neon platformer with time-loop mechanics",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "static-serve": "npx -y serve dist -p 4000",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "build:android": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "vite": "^5.4.0"
  }
}